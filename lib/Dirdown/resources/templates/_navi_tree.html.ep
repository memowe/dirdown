%#
% my $render_nav_tree;
% $render_nav_tree = begin
    % return unless my $items = shift;
    <ul>
    % for my $i (@$items) {
        <li>
            % (my $cp = $i->{cpath}) =~ s|^/||;
            %= link_to dirdown_page => {cpath => $cp} => begin
            % if ($i->{active}) {
                <strong><%= $i->{name} %></strong>
            % } else {
                %= $i->{name}
            % }
            % end
        </li>
        % if (exists $i->{children}) {
            %== $render_nav_tree->($i->{children})
        % }
    % }
    </ul>
% end
%#

<nav id="nav-tree">
    <h2>Navigation</h2>
    %= $render_nav_tree->($tree)
</nav>
