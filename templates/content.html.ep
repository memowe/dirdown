<p><strong><%= $content->name %></strong> (<%= $content->path %>)</p>

% if ($content->meta) {
    <p>meta:</p>
    <pre><%= dumper $content->meta %></pre>
% }

%== $content->html

<p>Everything:</p>
<pre><%= dumper $content %></pre>
